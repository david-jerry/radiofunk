from django.shortcuts import render
from .models import Stations

# Create your views here.
# def home(request):
#     podcasts = Tunes.objects.filter(tune_type=Tunes.PODCAST)
#     radios = Tunes.objects.filter(tune_type=Tunes.STATION)

